<h2>Edit a Project</h2>
<%= form_for @project, url: project_path(id: @project.id), method: :patch do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Save" %>
  <%= f.submit "Cancel" %>

<% end %>
